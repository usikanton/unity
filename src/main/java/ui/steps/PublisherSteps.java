package ui.steps;

import org.openqa.selenium.WebDriver;
import ui.pages.publisher.PublisherPage;

public class PublisherSteps {

    WebDriver driver;
    private PublisherPage publisherPage;

    /*public void openCreatePublisherForm() {
        publisherPage = new PublisherPage(driver);
        publisherPage.clickCreateNewPublisher();
    }

    public void verifyCreatedPublisher(String publisherName, String publisherEmail) {
        publisherPage = new PublisherPage(driver);
        publisherPage.verifyCreatedPublisher(publisherName, publisherEmail);
    }*/
}
